{
  "name": "frontend",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "fakeapi": "json-server --watch --port 3001 fakeapi/db.json"
  },
  "devDependencies": {
    "json-server": "^0.12.1"
  }
}
